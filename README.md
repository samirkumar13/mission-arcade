# Mission Arcade

Simple gamified task board with:
- XP and rank progression
- Add/delete custom tasks
- Local persistent memory (browser storage)
- Export/import memory backup (`mission-arcade-memory.json`) for moving progress across devices

## Run
Open `index.html` in a browser.

## Deploy (GitHub Pages)
Serve from the `main` branch root.
